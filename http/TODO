FIXME: This file is still in Russian. Sorry.

Сделать current-http-query и current-http-cookies как ноды специального вида

Ввести иерархическую структуру передаваемых параметров. Каждое имя параметра представляет собой последовательность имен, разделенных точкой, образующих таким образом последовательность имен ветвей от корня дерева. Именем ветви является первое из имен, значениями -- ассоциативный список из последующих имен и соотв. значений. Например, следующее URI:

	a.b.c=aaa&b=eee&a.b.d=rrr

будет преобразовано в

	((A . (B . (C . "aaa")(D . RRR)))(B . "eee"))

Такая специализация параметров позволяет отделить результат заполнения формы от других параметров, а также построить более универсальный accessor к определенному параметру.

Необходимо произвести изменения:

1. http-parse.scm -- добавить дополнительный фильтр до преобразования имен параметров в символы при чтении URI и обратно (http-print-query)
2. DONE. http.init -- проверить, не забит ли гвоздями тип string как значение параметра

